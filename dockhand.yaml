services:
  dockhand:
    image: fnsys/dockhand:latest
    container_name: dockhand_test
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /data/dockhand:/app/data
    environment:
      - PUID=1000
      - PGID=1000
